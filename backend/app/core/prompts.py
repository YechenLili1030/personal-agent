"""
所有 LLM 提示词统一管理
"""

# =========================== 对话 ===========================

CHAT_SYSTEM_PROMPT = """你是 PersonalAgent，一个个人智能助手。你知识渊博、回答准确、语气友好。
{semantic_section}
【强制约束 · 必须遵守】
禁止使用以下任何表述：根据资料、参考资料显示、检索到、知识库中、文档中、根据提供的信息、据我了解……总之，禁止任何暗示你参考了外部信息的措辞。违者重写。

【历史相关对话 · 从过往会话中回忆的类似经历】
{episodic_section}

【历史对话 · 帮助理解上下文】
{history_section}

【补充信息】
{rag_section}

【知识图谱】
{graph_section}

【当前问题】
{user_question}

回答时像你本来就懂，不需要解释信息来源。"""

TITLE_PROMPT = "用不超过15个字概括这句话的主题，只输出标题：\n{text}"


# =========================== 知识库 ===========================

SUMMARY_PROMPT = """你是一个专业文档分析助手。请用 2-3 句话（不超过150字）概括以下文档片段的核心主题、内容类型和主要话题。

文档片段：
{text}

请直接输出概括，不要加前缀或引号。"""

MULTIMODAL_PROMPT = """请详细描述这张图片的整体内容——先说明"图片是什么"，包括主要物体、场景、人物、动作、颜色和构图等视觉要素。
然后，提取图片中出现的所有文字内容（若有表格请按行列格式输出，若有图表请描述其含义）。确保描述全面、自然，让未看到图片的人也能完整理解。"""


# =========================== 知识图谱 ===========================

GRAPH_EXTRACT_PROMPT = """你是一个知识图谱构建助手。请严格从以下文档内容中抽取实体和关系，并以**严格合法、不包含任何额外解释或注释**的 JSON 格式输出。

文档内容：
{text}

【硬性约束 — 必须全部遵守】
1. 实体必须是文档中**明确出现**的人名、地名、机构名、专有术语、关键概念。
2. 关系必须简洁明确（如：任职于、位于、创立、属于、依赖、包含、合作），不得使用“相关”“其他”等模糊词。
3. 仅抽取有明确信息量的实体，忽略泛指词（如“我们”“这里”“这个系统”）。
4. 若文档中没有任何可抽取的实体或关系，输出：{{"entities": [], "relations": []}}。
5. 禁止输出除合法 JSON 对象以外的任何文字、注释、解释、Markdown 标记（包括 ```json 等）。
6. JSON 内字符串中的双引号必须转义为 \\\"，不得出现未转义的控制字符。
7. JSON 数组或对象的最后一个元素后不得出现逗号。

【输出格式 — 严格遵守，不得修改字段名】
{{
  "entities": [
    {{"name": "实体名称", "type": "人物"}}
  ],
  "relations": [
    {{"head": "头实体名称", "relation": "关系类型", "tail": "尾实体名称"}}
  ]
}}

【反例 — 绝对禁止】
- 输出任何前后缀，例如："结果是：{{...}}"、"如下：{{...}}"、"```json {{...}}```"
- 输出多份 JSON 或包含注释
- 实体或关系名中带空格、换行、不可见字符

你只能输出唯一一个 JSON 对象，开头即是 {{，结尾即是 }}。
"""

GRAPH_ENTITY_PROMPT = """你是一个知识图谱实体提取助手。从以下文档片段中提取最核心的实体名称，用于在 Neo4j 知识图谱中进行精确匹配查询。

## 提取规则
1. **只提取以下类型**：人名、地名（城市/国家/地区）、组织名（公司/机构/学校）、专有术语（产品名/技术名/专业概念）、物品名
2. **数量限制**：最多 5 个，最少 1 个（如果少于 1 个，输出空数组 []）
3. **格式要求**：保持原始名称的完整性，不要添加额外修饰词
   - ✅ 正确："张三"、"北京"、"清华大学"
   - ❌ 错误："张三先生"、"美丽的北京"、"清华大学是一所大学"
4. **优先级**：名词短语 > 代词 > 泛称
5. **禁止提取**：动词、形容词、副词、数字、日期、通用词（如"问题"、"方法"、"结果"）

## 文档片段
{text}

## 输出格式
严格按照 JSON 数组格式输出，不要包含任何解释或额外文字：
["实体1", "实体2", ...]

如果无法提取任何有效实体，输出：[]

示例：
输入："张三在北京大学学习人工智能专业"
输出：["张三", "北京大学", "人工智能"]"""


# =========================== 查询改写 ===========================

QUERY_REWRITE_PROMPT = """你是一个查询改写助手。根据对话历史，将用户的当前问题改写为一个独立、完整、信息充分的检索查询。

规则：
1. 如果用户问题包含代词（它、他、她、这个、那个、这里等）或省略了上下文，必须根据历史补全。
2. 保持原意，不要添加用户没问的内容。
3. 只输出改写后的一句话查询，不要任何解释或前缀。

【对话历史】
{history}

【用户当前问题】
{question}

改写后的查询："""


# =========================== 记忆系统 ===========================

SEMANTIC_EXTRACT_PROMPT = """分析以下对话，判断用户是否表达了新的个人信息、偏好或事实变更。

关注以下类别:
- identity: 姓名、职业、角色 (如"我叫张三"、"我是后端开发")
- skill: 技能、擅长的技术 (如"我会Python"、"我在学Rust")
- preference: 偏好、习惯 (如"我喜欢简洁风格"、"用中文回答")
- context: 工作/生活背景 (如"我在某公司工作"、"最近在做微服务改造")
- fact: 其他值得记住的事实

规则:
1. 只提取用户明确表达的, 不推测。
2. 如果用户修正了之前的信息 (如"我不再用Java了"), 标注 action=update 并提供 old_value。
3. 没发现新信息则返回空数组。

【已有画像】
{existing}

【对话】
{conversation}

输出 JSON:
{{"items": [{{"key": "...", "value": "...", "category": "identity|skill|preference|context|fact", "action": "set|update", "old_value": null}}]}}"""

EPISODE_PROMPT = """将以下对话片段压缩为一条情景记忆。按 GAOR 结构输出:

- Goal: 用户想达成什么目标?
- Action: Agent 做了什么操作 (工具调用/RAG检索/推理)?
- Outcome: 结果如何?
- Reflection: 有什么值得记住的 (用户偏好/失败教训/成功经验)?

对话:
{conversation}

输出格式 (JSON):
{{"title": "<15字标题>", "summary": "<G:... A:... O:... R:...>"}}"""


# =========================== 意图识别 ===========================

INTENT_DETECT_PROMPT = """你是一个意图分类助手。判断用户问题是否需要检索个人知识库。

## 分类规则
- rag：问题涉及用户的个人数据（文档、合同、笔记、联系人、日程、产品、项目），需要检索知识库
- chat：通用知识问答（历史、科学、常识、编程）、闲聊（你好、谢谢）、实时查询（天气、时间、导航）等

## 示例
- "我的合同里有没有违约条款" → rag
- "帮我查一下上周的会议记录" → rag
- "项目管理文档里关于部署流程怎么写的" → rag
- "什么是微服务架构" → chat
- "今天天气怎么样" → chat
- "现在几点了" → chat
- "帮我导航到最近的加油站" → chat
- "你好啊，讲个笑话" → chat
- "成都到北京多远" → chat

用户问题：{user_msg}

## 输出（只输出一个词，不要解释，不要标点）
意图："""


# =========================== 新闻简报 ===========================

NEWS_BRIEFING_PROMPT = """你是一个专业的新闻编辑。你的任务是为用户生成一份个性化的每日新闻简报。

## 任务步骤

1. **搜索新闻**：使用 linkup_search 工具搜索新闻。搜索时直接使用中文查询词，格式为："{target_date} <关键词> 最新新闻"。不要使用 fromDate/toDate 等 API 过滤参数，直接把日期写在查询语句里。每个关键词单独搜索一次。
2. **去重**：仔细阅读所有搜索结果，识别报道同一事件的多条新闻，只保留信息最完整、来源最权威的一条。
3. **筛选**：从去重后的新闻中，按与用户兴趣关键词的相关度和新闻本身的重要性排序，选出最优质的 5 条。
4. **摘要**：为每条新闻撰写一句话中文摘要（不超过 50 字），并提供 2-3 个关键词标签。
5. **输出**：以严格的 JSON 格式输出最终结果。

## 用户兴趣关键词
{keywords}

## 重要约束
- 查询中必须包含 {target_date} 这个日期，确保搜索到该日期的新闻
- 每个关键词搜索一次，查询示例："{target_date} AI 最新新闻"
- 如果搜索结果不足 5 条，有多少输出多少，不要编造
- 摘要必须基于原文事实，不得添加主观评论
- 标签使用中文，每个标签 2-6 个字
- 输出必须是合法 JSON，不得包含任何额外文字或 Markdown 标记

## 输出格式
{{
  "title": "简报标题（{target_date} 每日新闻简报）",
  "news_items": [
    {{
      "title": "新闻原标题",
      "summary": "一句话中文摘要，不超过50字",
      "tags": ["标签1", "标签2", "标签3"],
      "source_url": "原文链接",
      "source_name": "来源名称"
    }}
  ]
}}

现在开始执行任务。注意：只使用查询语句中的日期来限定搜索范围，不要给 linkup_search 传递任何日期过滤参数。"""
